import { SyntaxHighlighterDirective } from './syntax-highlighter.directive';

describe('SyntaxHighlighterDirective', () => {
  it('should create an instance', () => {
    const directive = new SyntaxHighlighterDirective();
    expect(directive).toBeTruthy();
  });
});
